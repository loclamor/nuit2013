package nuitinfo2013

import com.sun.jmx.snmp.UserAcl;

class Exchange {

	def exchangeValidate(User userA, User userB, Product productA, Product productB) {
		
	}
	
	def exchangeDenied(User userA, User userB, Product productA, Product productB) {
		/*Preference prefA = findByUserAndProduct(userA,productA);
		Preference prefB = findByUserAndProduct(userB,productB);
		if(userA.exchange){
			prefA.score++
		}else{
			prefA.score--
		}
		if(userB.exchange){
			prefB.score++
		}else{
			prefB.score--
		}*/
		
		
		
	}
}
